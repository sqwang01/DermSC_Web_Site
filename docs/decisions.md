# Decision Log

Settled decisions. Don't reopen these casually or mid-task to save effort. They can change
when the client asks, when a decision is found to contradict the reference docs or itself, or
when new facts make it wrong — record the change here with a new date and rationale rather than
editing history away.

| #   | Date       | Decision                                                                                                                                                                                                                                                                                                                                                                                                      | Rationale                                                                                                                               |
| --- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------- |
| D1  | 2026-08-28 | **Astro, static output**, vanilla CSS tokens, no CMS for v1                                                                                                                                                                                                                                                                                                                                                   | Fastest mobile load; strict copy governance suits developer-maintained content; add a headless CMS later only for an education/blog hub |
| D2  | 2026-08-28 | Host on **Vercel** (Pro), auto-deploy from GitHub `main`                                                                                                                                                                                                                                                                                                                                                      | Client preference; native Astro support; preview deploys per PR                                                                         |
| D3  | 2026-08-28 | At cutover, point **DNS at Vercel**, preserve MX records; registrar move to Cloudflare is optional and later                                                                                                                                                                                                                                                                                                  | Avoids breaking practice email; decouples hosting from registrar                                                                        |
| D4  | 2026-08-28 | **Callback-request forms only** — name, phone, email (optional), preferred time, service. No free-text medical detail. No PHI. Serverless function → Resend → `derm@dermsc.com`                                                                                                                                                                                                                               | Keeps the site entirely out of PHI/BAA scope; simplest compliance posture                                                               |
| D5  | 2026-08-28 | Analytics = **Plausible or Fathom**, not GA4                                                                                                                                                                                                                                                                                                                                                                  | Avoids the HHS tracking-technology / HIPAA problem with GA on a healthcare site; lighter, no cookie banner                              |
| D6  | 2026-08-28 | **New clean, keyword-aligned URL slugs** + a comprehensive **301 redirect map** for old WordPress URLs                                                                                                                                                                                                                                                                                                        | 301s preserve ~all link equity; new IA serves the four-category push and long-term SEO better than freezing old slugs                   |
| D6a | 2026-08-28 | `/emsculpt-neo/` (currently ranks) — keep-literal vs 301-to-`/cosmetic/emsculpt-neo/` **still open**                                                                                                                                                                                                                                                                                                          | Low stakes either way; decide at build time                                                                                             |
| D7  | 2026-08-28 | **Four top-level nav items:** General Dermatology · Skin Cancer · Cosmetic · GLP-1 Restore. Cosmetic + GLP-1 Restore carry the **brass** Restore sub-brand accent and "under a physician's direction" descriptor; the two medical sections stay pure evergreen                                                                                                                                                | Reconciles the practice's four-category push with the brand doc's endorsed sub-brand architecture                                       |
| D8  | 2026-08-28 | **Type-forward design** for launch; images are labeled placeholder blocks; swap in real photography later. **No stock, ever**                                                                                                                                                                                                                                                                                 | Photography not ready; stock would undo the positioning per style guide §7                                                              |
| D9  | 2026-08-28 | Providers on the site: **Moinfar, Lander, Fee, Falahati**. Dr. Mahsa Abdollahi removed everywhere                                                                                                                                                                                                                                                                                                             | Abdollahi no longer with the practice                                                                                                   |
| D10 | 2026-08-28 | SRT radiation device named as **Sensus SRT** on the skin cancer page                                                                                                                                                                                                                                                                                                                                          | Client confirmed the outstanding §14.2 item                                                                                             |
| D11 | 2026-08-28 | Referral line approved: "physicians in our community bring their own spouses and friends here." **No "complimentary" / "free" consultation language** anywhere                                                                                                                                                                                                                                                | Client confirmed P20; §6.2 P22 anti-kickback exposure avoided by dropping the word                                                      |
| D12 | 2026-08-28 | Old dermsc.com content is **not migrated verbatim** — pages written fresh to `docs/content-spec.md`; GLP-1 page uses the **cleared booklet copy**                                                                                                                                                                                                                                                             | Client: current site has no content worth keeping; booklet is legally cleared                                                           |
| D13 | 2026-08-28 | **No review count or review widget** at launch (revisit at 250+ reviews)                                                                                                                                                                                                                                                                                                                                      | Style guide rule #9; §6.4 digital-liability note                                                                                        |
| D14 | 2026-08-28 | Interactive features (e.g. a future quiz) ship as **Astro islands** (Preact/Svelte), not a second framework                                                                                                                                                                                                                                                                                                   | Keeps the zero-JS-by-default performance promise; no migration needed                                                                   |
| D15 | 2026-08-28 | Shopify store at `store.dermsc.com` is **out of scope** — linked as an external link only, untouched                                                                                                                                                                                                                                                                                                          | Client instruction                                                                                                                      |
| D16 | 2026-08-28 | **Claude Code drafts all page copy**, acting as the practice's marketing copywriter, to spec in `docs/content-spec.md`. Client reviews before publish; every page still runs the §11.3 pre-publication checklist and needs a client content proof at Phase 4                                                                                                                                                  | Client instruction; keeps the 4-week timeline; copy governance (§6/§8/§9) is documented well enough to draft against                    |
| D17 | 2026-08-28 | **Restore Membership placeholder pricing:** `$149/month` or `$1,490/year` (two months free). Placeholder inclusions: one priority annual skin exam, two skin-health visits/year, standardized photography baseline set yearly, member pricing on cosmetic + Restore services, priority scheduling / direct line. All clearly marked placeholder — client to confirm final number and inclusions before launch | Client asked for a placeholder to build against; real figure adjustable later without structural change                                 |
| D18 | 2026-08-28 | **No individual condition sub-pages at launch.** General Dermatology ships as one long-form page; conditions (acne; eczema/psoriasis/rosacea; rashes & hives; infections/warts/molluscum; hair/scalp/nails; pediatric; annual skin exams) fold in as a grouped, anchor-linked "What we treat" section, 2–4 sentences each. A dedicated condition library is deferred to the Phase 2 education/blog hub (D1) — see `build-plan.md` → "Post-launch backlog". | Medical side markets for authority, not acquisition (§1.2), and medical capacity is booked months out (§1.1 / §13); thin SEO condition pages would fight the type-forward positioning and add §9 claims-review surface against a 4-week timeline. Anchors (`#eczema`, …) give linkable targets and can map 1:1 to future sub-page URLs with 301s. |
| D20 | 2026-08-29 | **Dr. Lander's Mohs volume is now "more than 1,000 cases a year"** (was "more than 800"). Client confirmed the higher figure is current and defensible when the rewritten provider bios were added. Updated everywhere: Proof Bank P4 and the §4.1 / §6.1 / §12 boilerplate in `brand-positioning-v2.md`, the "numbers over adjectives" example in `style-guide.md` §"Numerals are the argument", `content-spec.md` (Lander bio meta + proof refs, home + skin-cancer proof lists), and the skin-cancer page copy (range item + referring-physicians block). The §6.1 caveat against publishing an *unconfirmed cumulative* career total still stands. | Client confirmed accuracy; keeps every instance of the figure consistent across copy, schema-feeding data, and reference docs. |
| D21 | 2026-08-29 | **Provider bio pages built from the client's provider-bios document.** `/about/<slug>/` pages now render real copy (lede, "How they practice", education, one client-supplied human detail, service + cross-links, CTA) from `src/data/site.ts` instead of the stub. Client-supplied portraits wired into the provider cards and bio pages via `astro:assets`. Kept per client sign-off: Dr. Moinfar's "Woman of the Year in Medicine and Healthcare, 2009", Silhouette InstaLift as a listed offering, the D11 physician-families referral line in her bio (approved wording), and the "top-ranked program / top of class" phrasing in both PA bios. Cure-rate/outcome language from the source draft was dropped (P21); Mohs method stated as methodology only. The About `/about/` team section now opens with the client's _"The people you'll actually see"_ intro (from the same document); the §12.6 continuity paragraph is held for the full About pass so the MA→PA line isn't repeated in one section. | Client added the bios + photos and asked to publish them across the site; flagged items cleared individually. |
| D22 | 2026-08-29 | **General Dermatology page leads with the practice's position; routine-access wording changed from "months" to "weeks" sitewide.** (1) Hero H1 is now `We look for the cause before we treat.` (matches the home-page General Dermatology door). The former H1 `Chronic doesn't mean unexplained.` moves down to become the heading of section 2 ("The problem"); its body is unchanged. Eyebrow unchanged (`Serious medicine. Natural results.`). (2) The **Pediatric dermatology** block was removed from the "What we treat" list per client; the `#pediatric` anchor is retired. The FAQ "Do you treat children?" is **kept** (still answers yes) so parents searching still land an answer. (3) The section-5 first-visit step "A written plan you leave with" was removed per client; the `MedicalProcedure` `howPerformed` on that page no longer says "written" plan. (4) Routine-appointment lead time is now stated as **weeks, not months** everywhere it appears in patient-facing copy — General Dermatology page (hero-adjacent access block, closing CTA, FAQ 1), Skin cancer page (closing CTA) — and in the reference docs: `brand-positioning-v2.md` §12.9 honest-access paragraph and "Asymmetry 3", plus every matching honest-limitation / honest-access note in `content-spec.md` (General Dermatology, Skin cancer, Home, Contact). Treatment-effect durations and hair-shedding timelines that also use "months" are unrelated and left alone. This supersedes the "booked months out" premise in D18's rationale (D18's no-sub-pages decision itself still stands). `content-spec.md` §"PAGE: General dermatology" updated to match. | Client instruction: the cause-before-symptom position is the page's argument and should be stated first, not buried in section 2; pediatric is being de-emphasized as a marketed line; and the practice's real routine wait is now weeks, so the old "months out" language across the site and strategy doc was no longer accurate. |
| D19 | 2026-08-29 | **Skin cancer page leads with detection and the full range, not Mohs.** Restructured so the page walks least-invasive → most: comprehensive skin exam + dermoscopy → PDT / field treatment → Mohs → Sensus SRT. Removed the standalone "How Mohs works" section (mechanism folded into the Mohs range item); "Candidacy" broadened from Mohs-only to "How the choice gets made"; hero eyebrow changed from `Serious medicine. Natural results.` to the plain label `Skin cancer`; the homepage "best treatment, not the most expensive one" line (§12.3) now also lands in the page's "Why here" block, attributed to experience + independence + range of options. Pathology framing simplified to "read by a board-certified dermatopathologist" — the P10 "Dr. Moinfar selected him personally, not by contract" wording was dropped from this page per client, as were the "kept a radiation unit / nobody upstream asked its margin" beat and the P20 spouses-and-friends line in the "Why here" section. `content-spec.md` §"PAGE: Skin cancer" updated to match. | Client: the old page over-indexed on Mohs and the tagline eyebrow was the wrong register for oncology; the practice's differentiator is offering the whole spectrum, tied to the independence argument, not any single procedure. |
| D25 | 2026-08-29 | **Cosmetic procedure pages show a one-item breadcrumb — the procedure name only, no `Home / Cosmetic` trail.** Applies to all ten cosmetic treatment pages: the seven that render through `src/layouts/RestoreTreatmentPage.astro` (neuromodulators, fillers, collagen-stimulators, laser, thread-lift, chemical-peels, microneedling) plus `coolsculpting`, `emsculpt-neo`, and `hair-restoration`, which call `<Breadcrumbs>` directly. Each now passes a single crumb (`[{ label: <procedure>, href: <path> }]`), rendered by `Breadcrumbs.astro` as the non-linked `aria-current="page"` label. `Breadcrumbs.astro` now emits the `BreadcrumbList` JSON-LD only when `crumbs.length > 1`, so these pages carry no breadcrumb schema (a one-item list carries no information). Multi-level trails elsewhere (provider bios `Home / About / <name>`, `Home / Privacy policy`, `Home / Accessibility statement`) are unchanged and keep their JSON-LD. `content-spec.md` breadcrumb notes for the seven face pages updated from `Breadcrumbs Home / Cosmetic / <x>` to `Breadcrumb: <x> only (D25)`; the fillers-page internal-links note that cited the breadcrumb as the route to the Cosmetic hub now points to the closing cross-sell link instead. | Client instruction: the breadcrumb on a cosmetic procedure page should just name the procedure — the `Home / Cosmetic` prefix is noise given the page's H1 and the site nav already place it. |
| D24 | 2026-08-29 | **Cosmetic face care split into seven single-modality pages; the Restore Membership program is removed (reverses D17).** (1) The combined `/cosmetic/injectables-and-laser/` page is replaced by seven brass sub-pages: `/cosmetic/neuromodulators/` (Botox, Dysport), `/cosmetic/fillers/` (hyaluronic acid), `/cosmetic/collagen-stimulators/` (poly-L-lactic acid / calcium hydroxylapatite biostimulators), `/cosmetic/laser/`, `/cosmetic/thread-lift/` (Silhouette InstaLift), `/cosmetic/chemical-peels/`, `/cosmetic/microneedling/`. All seven render through a new shared shell `src/layouts/RestoreTreatmentPage.astro` that bakes in the §11.2 section order and the verbatim §7.2 "why here" block (closing on §7.6 sentence 5), §12.8 pricing-integrity paragraph, and §12.10 consultation promise; each page supplies its own hero/offerings/candidacy/FAQ and its own `MedicalProcedure` + `FAQPage` schema. Candidacy/methodology language only — no percentages or outcome claims (§9.1). Laser device names and the delivering injector(s)/PA(s) remain `TODO(client)`. (2) The `/cosmetic/` hub "menu" is regrouped **For the face** (the seven) / **For the body and hair** (EmSculpt Neo, CoolSculpting, hair restoration); the membership card is gone. (3) **The Restore Membership is removed** — `/cosmetic/restore-membership/` page and its `content-spec.md` `### PAGE:` block deleted, `cta.membership` removed from `src/data/site.ts`, and every reference struck from `brand-positioning-v2.md` (§5.1 architecture diagram, §5.3 naming table, §7.3 message map, §12.11 CTA table, §13 measurement table, §14.3 open item 9, with a dated note at §5.1) and `content-spec.md` (page-list table + fixed-CTA table). (4) `redirects.md` + `vercel.json` add 301s `/cosmetic/injectables-and-laser/` → `/cosmetic/` and `/cosmetic/restore-membership/` → `/cosmetic/` (both slugs pre-launch, redirects are hygiene). (5) `build-plan.md` Phase 3 + open-items updated. Peels and microneedling were kept (not folded into laser, not dropped) per client. "Collagen building" is scoped to biostimulator injectables only — RF microneedling is explicitly out of scope. **Update (same day, client):** (a) the on-site lasers are **VBeam** (pulsed-dye — vascular/redness/rosacea) and **IPL** (pigment/photofacial); there is **no resurfacing/ablative/fractional laser**, so `/cosmetic/laser/` was rewritten around red-vs-brown (title, meta, offerings, candidacy, FAQ, schema, hub menu label "Laser & IPL") and the earlier "resurfacing" framing removed; (b) provider division of labour is set — **Dr. Moinfar assesses every patient and personally does filler, collagen stimulators, and threads; the physician assistants perform neuromodulators, microneedling, laser/IPL, and peels** — added as a required `whoPerforms` prop on `src/layouts/RestoreTreatmentPage.astro` and reflected on the hub ("Who you'll see" + FAQ). This resolves the `injector(s)/PA(s)` and laser-device TODOs D24 created. | Client instruction: each modality should be its own SEO page with candidacy copy specific to that treatment, rather than a single combined page; InstaLift threads and a distinct collagen-stimulator page are added to the line-up; and the Restore Membership is not launching, so the placeholder program from D17 is retired rather than carried as provisional. |
| D26 | 2026-08-29 | **"Who you'll see" on the six PA-delivered cosmetic pages now shows all three provider cards; the "Restore at Advanced Dermatology." hero subline is removed from every cosmetic page.** (1) Hero: the `<p class="hero__sub">Restore at Advanced Dermatology.</p>` line (and its now-dead CSS) is deleted from `src/layouts/RestoreTreatmentPage.astro` (all seven face pages) and from `src/pages/cosmetic/{index,coolsculpting,hair-restoration,emsculpt-neo}/index.astro`. The `Restore — under a physician's direction` eyebrow and the brass `restore` layout flag are untouched, so the section still reads as Restore. The identical line on the `/glp-1-restore/` pages is left in place. (2) "Who you'll see": on **neuromodulators, laser & IPL, chemical peels, microneedling, EmSculpt Neo, and CoolSculpting**, the section now renders **three `ProviderCard`s — Dr. Moinfar plus Madeline Fee PA-C and Shylie Falahati PA-C** — above the `whoPerforms` note (Dr. Moinfar assesses and sets the plan; one of the PAs performs the treatment). `RestoreTreatmentPage.astro` gets an optional `alsoSeenSlugs?: string[]` prop resolved against `providers`; a `team--multi` modifier switches the section from the one-card-beside-note layout to a cards row with the note beneath. The four face pages pass `alsoSeenSlugs={['madeline-fee-pa-c', 'shylie-falahati-pa-c']}`; `emsculpt-neo` and `coolsculpting` (standalone pages) inline the same two-PA list and layout, replacing their `TODO(client): name the treating provider` notes. **fillers, collagen-stimulators, thread-lift, and hair-restoration are unchanged** — Dr. Moinfar performs those herself, single card as before. `content-spec.md` "Who you'll see" notes for the six pages updated; this resolves the EmSculpt Neo / CoolSculpting treating-provider TODOs (the GLP-1 Restore muscle-track provider TODO is separate and still open). Extends D24's division-of-labour decision to the card display; no change to who does what. | Client instruction: name the treating providers on the pages the PAs deliver by showing their cards, and drop the redundant "Restore at Advanced Dermatology." subline under every cosmetic hero. |
| D27 | 2026-08-29 | **Cosmetic hub section 2 ("We would rather take less.") replaced with client-supplied "Our philosophy" copy.** The section keeps its slot and `#how-it-works` anchor (the "How it works ↓" hero CTA target) and its evergreen band; the H2 changes from `We would rather take less.` to `Our philosophy` and the body becomes three client paragraphs (2026-08-29): good cosmetic work can't be pointed to — people say you look well, not different — and every procedure is chosen and dosed toward that; it isn't one injection or one laser pass but a plan (what to treat, what to leave alone, how much, revisited over months) and the planning is the skill, the needle just carries it out; a board-certified dermatologist builds that plan and will recommend against a treatment rather than book it, which is why a physician is in the room before anything is scheduled. The supplied copy's run-in label "Our philosophy:" is promoted to the H2 rather than duplicated in the body. This **supersedes the `§7.6` sentences 2 and 4 verbatim quote** that ran in this section on the hub page only — §7.6 itself is unchanged and the sub-pages' §7.2/§7.6-sentence-5 "why here" blocks are untouched. `content-spec.md` §"PAGE: Cosmetic hub" section 2 (summary + drafted copy) updated; the stale "Restore at Advanced Dermatology." sub-head line in that page's spec (removed from the page itself in D26) was also struck. | Client instruction: the section should state the practice's cosmetic philosophy — restraint plus planning as the actual skill, with a physician gating what gets booked — in their own words. |
| D28 | 2026-08-29 | **"Why here rather than a storefront" on the cosmetic procedure pages replaced with client copy.** The section body on all seven face pages (via `src/layouts/RestoreTreatmentPage.astro`) and on `emsculpt-neo` becomes, verbatim: _"Dr. Moinfar assesses you. She won't book a treatment that's wrong for you, or more of it than you need — no one upstream sets a quota telling her otherwise. That's why you can trust it."_ Heading unchanged on the face pages; `emsculpt-neo`'s section heading changed from "Why here" to "Why here rather than a storefront" to match, its redundant fixed-protocol-photography sentence dropped (already covered by that page's Evidence section), and its device-neutrality sentence ("owns both EmSculpt Neo and CoolSculpting, no reason to steer you toward one") **kept as a second paragraph** so the P19 claim stays on the page. This supersedes the prior §7.2/§7.6-sentences-4–5 verbatim paragraph on these pages; it's a paraphrase within the same claim ("no one upstream sets a quota" ≈ §7.6 "no one here has a monthly target"; "that's why you can trust it" ≈ §7.6 sentence 5), so `brand-positioning-v2.md` §7.2/§7.6 are left as-is. `coolsculpting` has no "Why here" section (its device-neutrality point lives in the "CoolSculpting or EmSculpt Neo?" compare section) and `hair-restoration`'s "Why the diagnosis comes first" is a different argument — both left unchanged. `content-spec.md` "Why here rather than a storefront" notes for all seven face pages + `emsculpt-neo` updated. | Client instruction: state the commercial-independence point in Dr. Moinfar's terms — she won't book the wrong treatment or more than you need, and no quota pushes her to. |
| D29 | 2026-08-29 | **Cosmetic procedure pages get a `‹` back link to `/cosmetic/` in the breadcrumb slot.** The single-crumb breadcrumb on all ten cosmetic treatment pages (seven face pages via `RestoreTreatmentPage.astro`, plus CoolSculpting, EmSculpt Neo, hair restoration) now renders as a back link — `‹ <procedure name>` pointing at `/cosmetic/` — instead of the non-linked `aria-current` label D25 introduced. `Breadcrumbs.astro`: when `crumbs.length === 1` and that crumb has an `href`, it renders `<nav class="crumbs crumbs--back"><a href><span class="crumbs__chevron" aria-hidden>‹</span>label</a></nav>`; multi-crumb trails (provider bios, privacy, accessibility) are untouched and keep their JSON-LD; one-item trails still emit no `BreadcrumbList` schema. The four call sites now pass the **parent** href (`/cosmetic/`) instead of the page's own path. D25's intent (no `Home / Cosmetic` prefix, just the procedure name) still holds — the name is now also the link home. | Client instruction: each procedure page needs a visible way back to the overall Cosmetic page; a chevron in front of the procedure name in the breadcrumb slot. |
| D30 | 2026-08-29 | **The §12.8 line "The price is the price, and it doesn't go down if you wait." is removed from every cosmetic procedure page.** The consultation-&-pricing section on the seven face pages (`RestoreTreatmentPage.astro`), CoolSculpting, EmSculpt Neo, and hair restoration now quotes only §12.8's first sentence ("We don't run flash sales, holiday promotions, or discount packages."). The **Cosmetic hub** (`/cosmetic/`) section 5 still runs the full §12.8 paragraph. `brand-positioning-v2.md` §12.8 gets a dated note; `content-spec.md` per-page pricing notes updated from "§12.8 verbatim" to "§12.8 first sentence only (D30)". Also fixed in the same pass: the "Who you'll see" three-card row on the PA-delivered pages used an undefined `--space-10` token for its grid `gap` (the scale has no step 10 — style-guide §4), so the cards rendered with no gap; changed to `--space-12`. | Client instruction: drop the "price is the price / doesn't go down if you wait" line on the procedure pages; and the provider cards were packed together with no spacing. |
| D31 | 2026-08-29 | **New standalone careers page `/join-us/` built from the client careers brief.** One long-form Astro page (`src/pages/join-us/index.astro`), evergreen master-brand — **no brass** (the pipeline is public-facing continuity, not the internal succession/staffing plan kept 🔒 in §6.3). Sections: hero (H1 = P14, "Two of our physician assistants started here as medical assistants."), "How the path works" (exam-table apprenticeship; the "more than 100" figure = new Proof **P24**; Fee and Falahati named), "What the job actually is" (honest MA role description), "People leave this job. That's the point." (reframes tenure; closes on the approved §12.6 turnover line adapted), "Where they are now" (alumni gallery), "Who this is for" (good-fit / not-a-fit, mirrors the EmSculpt Neo candidacy pattern), "How to apply" (resume by email to `derm@dermsc.com`), closing CTA. **Deviations from the brief, all to honour locked rules:** (1) the alumni gallery is a **responsive grid, not a swipeable carousel** (design rule 5 bans carousels); (2) page copy is specced in `content-spec.md` as a `### PAGE:` block, **not** a standalone `join-us.md` (repo convention — copy lives in content-spec, pages are `.astro`); (3) the resume **upload** form from the brief is deferred — D4 keeps the site to callback forms with no file upload; applications go by email for now, full form spec is logged as an open item; (4) no `JobPosting` schema yet (stale-`datePosted` problem on a static site; logged as an open item). New files: `src/data/alumni.ts` (typed `AlumniEntry[]` — 2 real entries + 6 clearly-labelled `isSample` rows), `src/components/AlumniGallery.astro` (grid; **filters out any `consentOnFile: false` entry at build time** so a real name can't ship without a release; `photo: null` → dignified initials block, no stock/silhouette per style guide §7). Fee and Falahati are set `consentOnFile: true` on the strength of their existing site presence — confirming an actual signed release is a launch open item. `src/data/site.ts` footer "Practice" column gains a "Join us" link; `primaryNav` is untouched (still the four care sections). P24 added to the Proof Bank §6.1 ("more than 100" only — no precise count, no PA-vs-physician split). This resolves the `TODO(client): careers email or /careers/ page` placeholder in the About spec §5 — About's "Coming up through the practice" line should link to `/join-us/` when About is built. | Client supplied a careers brief: make the MA→PA/physician mentorship pipeline the headline of a recruiting page for the medical-assistant role, with an alumni "where they are now" gallery, in the practice's existing restrained voice. |
| D23 | 2026-08-29 | **General Dermatology "How we get the diagnosis right" (section 3) reframed around clinical judgment.** New client-supplied copy replaces the old §12.5 accuracy paragraph on the page: the fast way to practice dermatology vs. the slower one, judgment built from years of seeing the conditions on the page, Dr. Moinfar having **trained both physician assistants herself at the exam table before either saw a patient alone** (new Proof **P23**, the supervised-judgment companion to P14), how that shows up in a visit (full history, whole-skin exam, biopsy or labs when unclear), and "finding the reason is slower — it's also the only version of 'treated' that holds." The in-house-lab / personally-selected dermatopathologist / weekly second-read proof (P9/P10/P11) is **scoped out of section 3** and, in a same-day follow-up, **out of the hero lede** — the lede now closes on the position restated ("this is where we slow down and ask why it keeps happening — not just what will make it stop for now") rather than the dermatoscope/read-twice line. That proof still runs in the section-5 first-visit steps and the `MedicalProcedure` schema. **Hero eyebrow** changed from the `Serious medicine. Natural results.` tagline to the plain label `General dermatology` (same treatment D19 gave the Skin cancer page) — this supersedes the "Eyebrow unchanged" point in D22's item (1); D22's H1/section-2 restructure is untouched. No page now uses the tagline as its eyebrow. `brand-positioning-v2.md` §12.5 rewritten to the new paragraph with a dated note; P23 added to the Proof Bank (§6.1); `content-spec.md` §"PAGE: General dermatology" hero eyebrow + section 3 + Proofs list + drafted copy updated. The "six months with the same rash" line is a symptom-recurrence timeframe, not a routine-access lead time, so D22's "months"→"weeks" change does not apply (D22 explicitly left recurrence/treatment-effect timelines alone). The `TODO(client)` on "all three providers" is resolved for this section — the new copy doesn't use that phrasing (the Home-page check still stands). The **"Do you treat children?" FAQ was removed** per client — this reverses D22 item (2), which had deliberately kept that FAQ (answering yes) after the Pediatric block came out of §4; the `FAQPage` schema drops that Q&A, and no pediatric content now remains anywhere on the site. | Client instruction: the section should lead with how the practice's judgment is built — experience across these conditions and Dr. Moinfar's hands-on training of the PAs — rather than with the lab/pathology machinery, which the hero and first-visit steps still carry. Pediatric is no longer a marketed line at all, FAQ included. |
| D32 | 2026-08-29 | **`/join-us/` repositioned away from the pipeline-as-thesis: it now leads with the practice as a place to work and recruits openly for two role families — administrative staff and medical assistants, both permanent — with the MA→PA/physician training path kept as one highlighted section.** Supersedes the D31 framing (D31's build stands; its headline emphasis does not). Changes to `src/pages/join-us/index.astro`: (1) **H1** `Two of our physician assistants started here as medical assistants.` → `We're a small, physician-owned practice, and we're hiring.` (P14 moves into the "training path" section, no longer the page's lead claim). (2) **Hero lede** rewritten to the practice: founded 2006, physician-owned and independent, one building, ~17 staff, two board-certified dermatologists (one fellowship-trained Mohs, >1,000 cases/yr) + two PAs, weekly pathology review → "the reasoning behind a diagnosis gets talked through"; one soft line, "if you want to learn the medicine and not just get through the day, it's worth working here" (flagged as the page's only soft-sell, open item). (3) **Hero 2nd paragraph** (`.hero__hiring`, normal weight — not the muted `.hero__also` from the first pass): hiring for admin (front office) and MAs (exam rooms), "both can be long-term jobs", some MAs also use it as a step toward PA/med school. (4) New **section 2 "What we're hiring for"** (`--fog`, id `roles`): bolded lead-ins for each role family; MA copy — "some build a career in the role; others use it for a year or two of clinical experience before PA or medical school. We hire for both." (5) **"What the job actually is" → "The medical assistant role"**; tenure line "most people do it for a year or two before moving on to a program. That's the design, not a failure of it." → "some do it a year or two and move on to a clinical program; others make it a career. Both are what the role is for." (6) The old **"People leave this job. That's the point."** section is folded into a new **"The training path"** (`--evergreen`, id `the-path`, keeps `#the-path` anchor) — framed with "**also** been a place pre-PA and pre-med graduates come to get their clinical hours"; retains P24 "more than 100", Fee/Falahati (P14), candidacy-not-results support for leavers, and the approved §12.6 turnover line. (7) **"Who this is for"** — the D32-first-pass MA not-a-fit list is replaced with two plain "fits you if…" lead-ins (MA: career **or** pre-program; admin: no clinical background needed); no disqualifier list. (8) Hero CTA `How the path works ↓` → `The training path ↓`. (9) **Title** `Medical Assistant Careers in Dana Point` → `Admin and medical assistant careers`; **meta** rewritten to the practice + both role families + "pre-PA training path". Alumni gallery, consent enforcement, "How to apply" (both subject tags), and closing CTA unchanged from the D32 first pass. **Reference docs:** `brand-positioning-v2.md` §7.3 message-map **Careers** row updated — headline idea `The pipeline` → a small practice worth working at, hiring admin + MAs; the training path is now *lead proof*, not the headline (local/gitignored copy; no Proof Bank entry reversed — P1/P14/P23/P24 all still stand). `content-spec.md` "PAGE: Join us" header + sections list + open items rewritten. **Still no invented facts:** front-office titles/functions generic, no front-office tenure claim, no MA average-tenure number, "over 100" only. (10) **Breadcrumb removed** (`Home / Join us`) per client — the hero opens straight on the eyebrow + H1; the `BreadcrumbList` JSON-LD goes with it (`Breadcrumbs` component is dropped from the page, `.hero__copy` top margin zeroed). | Client, follow-up to the same day's first pass: the practice wants permanent medical assistants too, not only students passing through — so the page should open with the overarching benefit of working here and state plainly that it's recruiting for admin and MA roles, then highlight the training path and track record as one distinctive thing rather than the whole pitch. |
| D33 | 2026-08-29 | **The "We don't run sales" section is removed from the Cosmetic hub (`/cosmetic/`) — completing D30.** D30 dropped the §12.8 pricing-integrity paragraph from every cosmetic procedure page but explicitly kept the full paragraph as section 5 of the hub; the client has now asked for it to come off the hub as well. The hub section order is re-numbered 1–8 (was 1–9): "The consultation" (§12.10 verbatim) is now section 5, FAQ section 6, closing CTA section 7, cross-sell section 8. `content-spec.md` "PAGE: Cosmetic (hub)" — both the section outline and the drafted-copy block — updated to match. `brand-positioning-v2.md` §12.8 is local/gitignored; the paragraph itself is unchanged there as source copy, it is simply no longer surfaced on any built page. | Client instruction: remove the "we don't run sales" section from the cosmetic page. |
| D34 | 2026-08-29 | **The P19 device sentence on `emsculpt-neo` ("Why here rather than a storefront", 2nd paragraph) is reframed from neutrality to capability.** Was, per D28: _"The practice owns both EmSculpt Neo and CoolSculpting and has no reason to steer you toward one over the other."_ Now: _"The practice runs both EmSculpt Neo and CoolSculpting, so Dr. Moinfar can match the device to what your body actually needs — reducing a pinchable bulge, building muscle, or both."_ The both-devices fact is retained; the conclusion leads with matching the device to the patient's need rather than with commercial neutrality. Proof Bank **P19** wording in `brand-positioning-v2.md` updated to match (capability-first, neutrality kept as a trailing note). D28's "device-neutrality sentence kept as a second paragraph" instruction stands except for this rewording. `content-spec.md` `emsculpt-neo` "Why here rather than a storefront" note updated. | Client instruction: keep the point that the practice has both devices, but conclude that the practice has the right device for the patient's need — not that it is indifferent between them. |
| D35 | 2026-08-29 | **`coolsculpting` hero and "CoolSculpting or EmSculpt Neo?" section reworked to client copy.** Hero (per client): H1 _"Freeze the fat, not the muscle."_, lede _"CoolSculpting targets a pinchable bulge in cleared areas with controlled cooling — no surgery, no downtime."_ — this drops the hero's "not a weight-loss treatment" clause (still carried by the procedure schema and the candidacy list) and its P19 "we offer both … no incentive to steer" clause. In the compare section: the note paragraph _"We own both and have no incentive to recommend one over the other. The consultation is about which one matches what you want to change — or whether neither is right."_ is **removed**; the "Read about EmSculpt Neo" link moves under the EmSculpt Neo compare item; the section CTA is reduced to a single primary button, "Schedule a consultation" → `/contact/` (a briefly-added "Ask our staff" `tel:` button was removed at client request). Net effect on P19 for this page: the neutrality point now appears only in the FAQ "CoolSculpting or EmSculpt Neo?" answer ("We own both") and the meta description; **P19 supersedes D28's placement of the neutrality sentence in the compare section.** `brand-positioning-v2.md` P19 note and `content-spec.md` `coolsculpting` section 5 (outline + drafted copy) updated. | Client instruction, given step by step while editing the page. |
